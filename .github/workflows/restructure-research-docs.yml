name: Restructure research documentation

on:
  push:
    paths:
      - ".github/workflows/restructure-research-docs.yml"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Smoke test
        run: echo "workflow-ok"
